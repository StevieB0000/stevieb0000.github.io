cd path/to/ironpeak-gym
git init && \
git add . && \
git commit -m "Initial commit - IronPeak Gym website" && \
git branch -M main && \
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git && \
git push -u origin main
